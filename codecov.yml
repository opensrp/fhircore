codecov:
  disable_default_path_fixes: yes

coverage:
  status:
    project:
      default:
        enabled: yes
        target: auto
        threshold: 1%
    patch:
      default:
        target: 60%
  precision: 1
  round: down
  range: "75...100"

comment:
  layout: "header, diff, changes, uncovered, tree"
  behavior: default

flags:
  engine:
    paths:
      - "/Users/runner/work/fhircore/fhircore/android/engine"
  geowidget:
    paths:
      - "/Users/runner/work/fhircore/fhircore/android/geowidget"
  quest:
    paths:
      - "/Users/runner/work/fhircore/fhircore/android/quest"
